- name: Run container
  run: |
    CONTAINER_ID=$(docker run -d -p 5000:5000 myapp:${{ github.run_number }} || true)
    if [ -n "$CONTAINER_ID" ]; then
      docker logs $CONTAINER_ID
      docker stop $CONTAINER_ID
    else
      echo "Container failed to start"
      exit 1
    fi
